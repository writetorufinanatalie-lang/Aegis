# AEGIS — GMS Processors Pvt Ltd
# Copy to .env for local development
# Frontend env (Vite) — PUBLIC, no secrets
VITE_API_BASE_URL=http://localhost:8000
VITE_FACTORY_ID=GMS-TRP-001

# Backend env — server-only, never expose to frontend
# FINANCIAL_PROVIDER=simulated
# FINANCIAL_PROVIDER_SECRET=  # only for future regulated provider, keep server-side

# Optional
PORT=8443
